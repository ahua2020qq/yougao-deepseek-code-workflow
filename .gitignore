# Node.js依赖文件夹（核心忽略项，重点！）
node_modules/

# 编译/打包输出目录（你的deepseek项目的out/、dist/，AAA项目若有也会忽略）
out/
dist/
build/

# VSCode插件打包文件（deepseek项目的.vsix文件，不用上传）
*.vsix

# 日志文件
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 环境变量配置文件（若有，包含敏感信息，不要上传）
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# VSCode本地配置（个人编辑器设置，不用共享）
.vscode/
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json

# 操作系统临时文件
.DS_Store
Thumbs.db

# 本地测试临时文件（已上传的helloyougao.txt及后续测试生成的文件）
helloyougao.txt
calc.js
*.test.tmp
# PowerShell测试脚本执行产物
*.ps1.xml
